global:
  scrape_interval:     10s
  evaluation_interval: 10s

  # Attach these labels to any time series or alerts when communicating with
  # external systems (federation, remote storage, Alertmanager).
  external_labels:
      monitor: 'docker-heplify-server'

# Load and evaluate rules in this file every 'evaluation_interval' seconds.
rule_files:
  - "alert.rules"

# A scrape configuration containing exactly one endpoint to scrape.
scrape_configs:
  - job_name: '{{ tenant_folder }}_app_host'
    static_configs:
      - targets: ['{{ application_host }}:9100']

  - job_name: '{{ tenant_folder }}_voice_host'
    static_configs:
      - targets: ['{{ voice_host }}:9100']

  - job_name: prometheus
    static_configs:
      - targets: ['localhost:9090']

  - job_name: '{{ tenant_folder }}_heplify-server'
    static_configs:
      - targets: ['{{ omni_ip_lan }}:9096']

  - job_name: '{{ tenant_folder }}_postgres'
    static_configs:
      - targets: ['{{ application_host }}:9187']

  - job_name: '{{ tenant_folder }}_redis'
    static_configs:
      - targets: ['{{ application_host }}:9121']

  - job_name: '{{ tenant_folder }}_asterisk'
    static_configs:
      - targets: ['{{ voice_host }}:7088']

alerting:
  alertmanagers:
  - scheme: http
    static_configs:
    - targets:
